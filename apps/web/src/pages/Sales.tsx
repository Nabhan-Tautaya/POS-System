import "../styles/Sales.css";

export default function Sales() {
  return (
    <div className="page">
      <div className="page-header">
        <h1>Sales</h1>
        <p>Checkout / POS screen — this is where the register UI plugs in.</p>
      </div>

      <div className="page-placeholder">Checkout interface goes here.</div>
    </div>
  );
}
